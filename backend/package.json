{
  "name": "stockloom-backend",
  "version": "1.0.0",
  "private": true,
  "description": "StockLoom database + backend layer (Express + parameterized SQL over PostgreSQL).",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/api/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
